import React, {Component} from 'react';
import {Platform, StyleSheet, Text, View} from 'react-native';

class ExamsScreen extends Component {
  render() {
    return (
        <Text>Exams</Text>
    );
  }
}

const styles = StyleSheet.create({
  
});

export default ExamsScreen;